{% extends 'base.html.twig' %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>


<h1 class="text-center">🙈 Voici un magnifique 🙉 BLOG🦄</h1>
<div class ='d-flex justify-content-center flex-wrap m-5'>

{% if article.id is divisible by(2) %}
<div class="card border-secondary mb-3" style="max-width: 20rem;">
  <div class="card-header">{{article.title}}</div>
    <img class="fit-picture" src="{{article.image}}" alt="Grapefruit slice atop a pile of other slices">
    <rect width="100%" height="100%" fill="#868e96"></rect>

  <div class="card-body">
    <h4 class="card-title text-center">{{article.title}}</h4>
    <small class="text-muted">{{article.createdAt|date("d/m/Y")}}</small>
    <p class="card-text">{{article.contenu}}</p>
  </div>
  <button type="button" class="btn btn-secondary m-5 mb-1 mt-1"><a href="../acceuil">Revenir en arrière</a></button>
{% else %}
    <div class="card border-primary mb-3" style="max-width: 20rem;">
  <div class="card-header">{{article.title}}</div>
    <img class="fit-picture" src="{{article.image}}" alt="Grapefruit slice atop a pile of other slices">
    <rect width="100%" height="100%" fill="#868e96"></rect>

  <div class="card-body">
    <h4 class="card-title text-center">{{article.title}}</h4>
    <small class="text-muted">{{article.createdAt|date("d/m/Y")}}</small>
    <p class="card-text">{{article.contenu}}</p>
  </div>
  <button type="button" class="btn btn-secondary m-5 mb-1 mt-1"><a href="../acceuil">Revenir en arrière</a></button>
</div>
</div>
{% endif %}        
{% endblock %}