{% extends 'base.html.twig' %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div>

<div class="card m-5">
  <div class="card-body">
    <h4 class="card-title">{{category.title}}</h4>
    <h6 class="card-subtitle mb-2 text-muted">créer le :{{category.createdAt|date("d/m/Y")}}</h6>
    <p class="card-text">{{category.description}}</p>

  <h4 class="card-title">liste des articles :</h4>
{% for item in category.articles %}<p class="card-text">- {{item.title}} : disponible <a href="{{path('app_show', {'id':item.id})}}" class="card-link"> ici</a></p>{% endfor %}

    <a href="{{path('app_category')}}" class="card-link">revenir category</a>
  </div>
</div>

</div>
{% endblock %}